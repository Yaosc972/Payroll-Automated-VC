# Sigma Workbench 接管审计记录

> 日期：2026-06-09
> 范围：招聘奖金核算、国内劳务工薪酬核算、海外劳务工报账核对、FBU 美洲绩效奖金核算
> 目标：停止多窗口并行发散，统一进入可测试、可发布、可追溯的产品线接管流程。

## 1. 当前总体判断

当前项目已经从单一招聘奖金工具扩展为多模块工作台，但各模块成熟度不一致。招聘奖金核算是最接近可交付的主模块；国内劳务、海外劳务、FBU 绩效均已有代码与页面，但仍需要按业务规则、真实数据和测试稳定性逐项收口。

接管原则：

- 不再让多个开发窗口同时修改同一批核心文件。
- 所有规则修改必须先有业务来源、样例数据和回归对比。
- 所有模块优先做到“能解释、能复核、能导出、能留痕”，再追求自动化程度。
- AI 可用于抽取、解释和异常辅助，但金额计算必须由确定性规则引擎完成。

## 2. 模块状态

### 2.1 招聘奖金核算

状态：主模块，优先保持稳定。

已具备：

- 月度导入、初算、待确认、最终结果、差异核对。
- Command Center 明细表、筛选、批次化、下载结果。
- Excel 导出与平台内可查的核算明细。

接管重点：

- 保持招聘奖金规则稳定，不因其他模块开发产生回归。
- 后续规则变化必须先进入独立测试样例，再合并。
- 继续优化差异报告和明细解释，但不随意改金额计算。

### 2.2 国内劳务工薪酬核算

状态：功能框架完整，但需要真实数据验证。

交接来源：

- `HANDOFF_DOMESTIC_LABOR_PAYROLL.md`
- `bonus_platform/engine/domestic_labor/`
- `bonus_platform/static/domestic-labor.html`
- `bonus_platform/static/domestic-labor.js`

已具备：

- 全勤奖、餐补、外宿补贴、工龄奖四个引擎。
- 文件解析、模板下载、后台任务、结果导出。
- API 测试和页面入口。

主要风险：

- 餐补、外宿补贴、工龄奖依赖真实考勤、住宿、区域和岗位字段，模板样例不足以证明规则正确。
- 工龄奖是否允许负数、全勤奖事假阈值、区域识别方式仍需业务确认。
- 数据校验层还不够强，错误模板可能导致结果看似完成但业务不可用。

下一步：

1. 收集一份真实月度劳务薪酬样本和线下结果。
2. 做端到端差异报告，而不是直接上线。
3. 将规则假设转成“需确认项”并在界面或报告中暴露。

### 2.3 海外劳务工报账核对

状态：能力重、风险高，需要先降复杂度。

交接来源：

- `HANDOFF_OVERSEAS_LABOR_RECONCILIATION.md`
- `HANDOFF_AI_OPTIMIZATION.md`
- `bonus_platform/engine/labor/`
- `bonus_platform/static/overseas-labor.html`
- `bonus_platform/static/overseas-labor.js`

已具备：

- 多 PDF/Excel 上传。
- Excel 字段映射向导。
- PDF 文本/图片抽取、员工名清洗、金额比对、仓库维度比对。
- Profile 管理和质量诊断。

主要风险：

- AI 抽取速度慢，Token plan 场景可能 20-30 分钟。
- 员工名匹配率仍可能受 PDF 格式影响。
- 动态 Profile 存在污染风险，错误格式可能影响后续批次。
- 默认并行配置必须谨慎，当前已恢复为测试期望的 `parallel_max_workers=1`。

下一步：

1. 先做“确定性 Excel 核对”和“PDF 抽取质量报告”分层，不把 AI 结果直接当最终答案。
2. 加后台任务超时和可重试机制。
3. 对每个供应商建立固定样本和可复核 Profile。

### 2.4 FBU 美洲绩效奖金核算

状态：页面和流程已搭好，但规则仍需核验。

交接来源：

- `HANDOFF_FBU_AMERICAS_PERFORMANCE_BONUS.md`
- `bonus_platform/engine/fbu_performance/`
- `bonus_platform/static/fbu-performance.html`
- `bonus_platform/static/fbu-performance.js`

已具备：

- 月度活动、考勤导入、薪资导入、绩效导入、花名册集成。
- 基数、系数、奖金计算。
- 数据预览、筛选、导出。

主要风险：

- 48h 单独核算人员、病假余额结算、非仓人员绩效系数、区长计算方式仍需确认。
- 当前准确率说明来自样本比对，但还不能替代完整规则验收。
- 花名册每次上传，尚无版本化基础数据管理。

下一步：

1. 固化 202604 样本回归测试。
2. 把待确认规则做成显式异常或规则开关。
3. 设计全局花名册管理，避免每次重复上传。

## 3. 测试状态

接管前全量测试曾出现 1 个失败：

- `tests/test_labor_api.py::test_parallel_image_render_workers_config`
- 原因：`AI_CONFIG["parallel_max_workers"]` 被改为 2，测试期望为 1。
- 处理：恢复默认值为 1，后续如果需要提速，用环境变量或独立配置开关控制。

接管提交前必须重新运行：

```bash
python3 -m pytest
```

目标状态：

- 全量测试通过。
- 临时 `.bak/.bak2` 文件不入库。

## 4. 优先级路线

P0：

1. 保持招聘奖金主流程稳定。
2. 收口交接文档和测试失败。
3. 禁止多窗口同时修改核心文件。

P1：

1. 国内劳务使用真实样本做差异验证。
2. FBU 绩效确认四个高风险规则点。
3. 海外劳务增加超时、重试、质量分层。

P2：

1. 统一四个模块的批次、文件、异常、导出体验。
2. 抽象公共上传/表格/下载组件。
3. 建立规则版本和样本回归库。

