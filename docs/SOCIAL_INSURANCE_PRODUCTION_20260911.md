# 社保完整更新生产发布记录（2026-09-11）

## 基线与集成

- 实际生产发布线：`codex/domestic-production-20260909`，工作区 `/Users/zt27532/Documents/sigma-domestic-production-20260909`。
- 发布前实际工作台版本：`63ac5eca4c33e746d1afcf3e5e91cba86c6f9f26`，部署 `dpl_ALqnK1nceqrdDscTHvbz5czQzU6d`。
- 源提交：`dc7bea36e3c7aa3bf02fd34a989eb0c7edf8fd92`，以前序 `93d53a24` 为基线；选择性 cherry-pick 为 `bfff48b1`，没有覆盖其他模块。
- 原工作区两处定时补丁保存为 stash `preserve pre-existing social cron patches before dc7bea36 integration`；集成后文件与该补丁完全一致，无需重复应用。
- 工作台规则包与生产连接器统一使用 `2026.09.10-01`；两端原显式环境版本均为 `2026.08.27-07`，发布时必须同步更新。
- 每天北京时间 08:00：`/api/social-insurance/cron/refresh`，UTC `0 0 * * *`。代码配置不代替 Vercel 实际登记验收。

## 发布前数据状态

- 成功名单版本 `release_20260903082821_0c464589`，发布时间 `2026-09-03T08:28:21.912807Z`。
- 33 个主体、175 人；32 个草稿批次、1 个已确认批次。已记录各历史批次的摘要与内容哈希用于上线后比对，不在仓库保存人员数据。
- 周期 `2026-07-16` 至 `2026-08-15`，确认日 `2026-08-16`；这是 9 月 11 日最近完整结束的周期。
- 默认主体缺少有效政务模板，不能用审核清单或自造空表替代真实报盘验收。

## 回滚点

- Git 标签：`rollback/social-insurance-20260911-63ac5eca`。
- 旧工作台：`https://sigma-workbench-7aykr6xee-yaosc-s-project.vercel.app`。
- 旧生产连接器：`https://sigma-social-insurance-connector-7b5oxl55a-yaosc-s-project.vercel.app`，部署 `dpl_23tLJttiZ6EPS7tBXFpYzxWUvVyY`，提交 `f68000959b65e56ed876ff3315d5914374151148`。
- 旧工作台连接器地址配置：`https://sigma-social-insurance-connector.vercel.app`。
- 配套恢复两端旧部署及原环境配置；不得只回滚一端造成规则版本不匹配。密钥保持不变。
- 部署回滚不回写名单、人工确认和模板，也不自动回退成功名单指针；数据问题需要另行核对具体发布版本，禁止覆盖历史人工确认。

## 验收边界

- 授权仅额外执行一次生产同步；若返回不确定，先查成功发布指针和日志，不盲目重试。
- 全部主体导出仅为审核清单，政府系统最终提交保持禁用。
- 最终部署 ID、同步结果和未解决事项以发布后的验收补记为准。

## 首轮生产验收与补丁

- 工作台 `4544f282` 部署 `dpl_DRuQfU6zaguxUQQXiWhQCshueZyo`，连接器部署 `dpl_BK2Q7gztjnmbqaekv6vGSHCVwep7`，两端规则 `2026.09.10-01`。
- 连接器沿用正式域名 `https://sigma-social-insurance-connector.vercel.app`。独立部署地址受访问保护，未采用该地址作为生产服务间调用配置，未放宽保护。
- Vercel 项目已登记 Cron：`0 0 * * *`，每天北京时间 08:00。
- 仅执行一次 `vercel crons run /api/social-insurance/cron/refresh --scope yaosc-s-project`，触发时间 `2026-09-11T06:26:34.378Z`。请求 `pcv2w-1789107994629-edf558f2cc1c` 返回 200，状态 ready，耗时 192592ms。
- 新成功名单 `release_20260911062946_352986cc`，发布于 `2026-09-11T06:29:46.105937Z`，34 主体、176 人；171 可报盘、5 待确认。默认周期仍为最近完整结束的 `2026-07-16` 至 `2026-08-15`，确认日 `2026-08-16`。
- 与旧版匹配的175人中，3人的户籍分类、63人的户口具体地址改变。1条“户籍所在地深圳、户口地址外地”记录仍为旧分类；该主体复用15条历史基线，需要单独核对，不能宣称新规则覆盖所有历史记录。
- 旧33批次首次上线后内容哈希全部一致，历史人工确认未被改写。真实义乌主体新批次 `sir_20260911062941_141b209e` 的4人已确认，刷新后状态和上传模板记录保留。
- 真实模板来源为既有资料库的 `2608/社保人员批量参保信息表模板-云途义乌.xlsx`，不是自造模板。预检通过，但首次实际生成返回409“批次未能保存到持久化存储”。生产配置实为Supabase，中文报盘文件直接充当对象键，与同平台外包工模块此前问题相同。
- 为此追加社保专用的可移植对象键及原名恢复映射，并确保文件先于批次文档保存。回归测试先复现失败，再验证修复；社保与静态页面测试257项通过。初始全量回归2133通过、13跳过，连接器19通过。此补丁不再次执行生产同步。

## 最终生产版本与实际验收

- 工作台生产提交：`86f012b7056876c2fcaae5118e2d8ef5cab4a66f`，已推送 `origin/codex/domestic-production-20260909`。
- 正式地址：`https://sigma-workbench.vercel.app`；部署地址 `https://sigma-workbench-ly4rfyp62-yaosc-s-project.vercel.app`，部署 ID `dpl_Lp8cxkTvw9y4q1LobEemzgNR3Q77`。正式域名实际指向此 READY 部署。
- 连接器提交：`4544f28204ce7400285f347d99b12f9199df728e`，部署地址 `https://sigma-social-insurance-connector-riwgj3jop-yaosc-s-project.vercel.app`，部署 ID `dpl_BK2Q7gztjnmbqaekv6vGSHCVwep7`。正式连接器域名指向此 READY 部署，健康检查通过；后续工作台存储修复不涉及连接器代码。
- 两端显式规则版本 `2026.09.10-01`；连接器 `rules.mjs` SHA256 为 `855a901b9dabe11cf7c112fb28b4be5c98b18733afac153276e1db2f53779b7b`。
- 最终生产项目 Cron 登记关联上述工作台部署，`disabledAt: null`，路径 `/api/social-insurance/cron/refresh`，UTC `0 0 * * *`，即每天北京时间 08:00。下一次自然定时执行为 2026-09-12 08:00，验收时尚未发生；本次验证的是唯一一次人工触发的实际生产 Cron 请求。
- 生产同步成功时间为北京时间 2026-09-11 14:29:46，成功名单仍为 `release_20260911062946_352986cc`，34 主体、176 人。原 33 个历史批次在本次同步后的完整内容哈希全部一致，包括原已确认批次。
- 全部主体页面显示 176 行、主体列及进入具体主体入口；全部主体模式禁止整批确认。实际点击行内入口能切换对应主体。已验证搜索及待确认筛选。
- 实际下载新版全部主体审核清单：人员表 180 行（4 行表头说明、176 人）、56 列；主体汇总 35 行（1 行表头、34 主体）、5 列。该文件不是政府申报文件。
- 义乌真实模板报盘生成返回 200，生成时间 `2026-09-11T06:46:55.145613Z`，4 人、1 条浙江合并路径、无被阻断路径。刷新后重新选择义乌主体，已确认状态、已上传模板和下载入口均恢复；实际下载 ZIP 成功。页面刷新默认选择首个主体，不代表已保存批次丢失。
- 下载包内 `报盘文件/浙江社保医保合并模板_4人.xlsx` 的两行原模板表头保持一致；4 条唯一人员记录全部匹配当前批次，没有夹带旧模板人员。ZIP 同时包含审核资料和生成说明。未执行政府系统最终提交。
- 工作台首页、社保页面、国内劳务页面等生产页面请求返回 200；页面缓存版本为 `social-insurance.js?v=49`。最终社保测试 173 项通过，配套静态测试 84 项通过；连接器 19 项通过。初始全量测试与最终定向测试分别记录，不把初始全量结果冒充存储补丁后的全量复跑。

## 未解决事项与回滚注意

- 户籍规则实际验证仍有 1 条例外：批次 `sir_20260911062943_becf351c` 中，源户籍所在地为深圳、户口地址为外地的记录保留旧分类。该批次复用了 15 条历史基线人员，需要进一步确认该记录的来源及是否应重新计算；未擅自改写历史人工确认，也未再次触发同步。不能宣称所有历史记录均已按新规则重算。
- 本次真实政务模板验收覆盖义乌浙江合并路径，不代表其他城市路径均已验收。缺少有效模板的主体仍需提供实际模板，不能以全部主体审核清单或自造模板替代。
- 回滚使用上方旧工作台、旧连接器部署配套恢复，两端显式规则版本恢复 `2026.08.27-07`，连接器正式地址不变、密钥不变。Git 回滚点已推送。
- 新版中文报盘文件使用可移植对象键，旧代码不具备对应恢复映射；回滚旧工作台后，新生成报盘包可能无法读取，应事先保留所需下载，不能通过删除或覆盖新旧批次解决。
- 本文件为发布后验收补记；文档提交可以晚于生产代码提交，实际生产代码以 `86f012b7` 和 Vercel 部署元数据为准。
