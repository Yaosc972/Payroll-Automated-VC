# 社保报盘业务反馈修复交接（2026-09-10）

## 分支与范围

- 修复分支：`codex/social-insurance-feedback-20260910`。
- 基于本地生产发布分支 `codex/social-insurance-production-release-20260827` 的 `93d53a24`；发布前仍须与当前集成分支合并核对，不可直接以本分支覆盖后续生产变更。
- 原开发窗口有无关未提交修改，本次使用独立 worktree，未改动原窗口。
- 本文随开发提交交付；尚未推送、部署或修改远端配置、密钥、数据库及生产入口。

## 业务来源

- 陈澜群消息：`om_x100b66a7dc7198a8dfa99ebb643367b`，2026-09-03 17:08（17:10 更新），群 `oc_05e1698c317023a8adb0b0a4d97cbc13`。
- 反馈：全部主体名单不能一起下载；确认人员后不能生成批量增员模板；更新户口地址/户籍所在地匹配规则。
- 讨论文档：<https://ztn.larkenterprise.com/docx/OdsTdTgdaoty2exkYhBcx8wpncc>。
- 户籍所在地深圳、户口地址明确在外地：讨论明确深户分类，未明确地址如何覆盖。本次保留原地址并提示人工核对。

## 已实现（开发测试通过，未云端验收）

1. 增加“导出全部主体名单”：使用当前成功集成版本引用的所有主体批次，读取各主体最新审核结果，输出全员明细和主体汇总（含零人主体）。周期、确认日或主体不一致时拒绝混合导出；保留现有权限检查。此文件是审核名单，不是政府申报文件。
2. 确认名单仅读取、保存名单文档，不再遍历恢复和上传模板及历史导出文件。这减少了无关文件操作，但不代表已证实或消除 9 月 3 日的云端存储根因。
3. 已上传模板的页面识别使用持久化上传记录，不因当前云端实例没有本地文件而误判未上传。实际生成仍要求恢复并验证真实模板。
4. 户口地址优先；行政区域缺失时补齐，保留详细地址；户口地址、户籍所在地任一处深圳即判深户；冲突地址不静默改写。增加六个地址回归用例。
5. 工作台与连接器规则版本更新为 `2026.09.10-01`。如果部署环境显式设置了 `SOCIAL_INSURANCE_RULE_VERSION`，发布时需要同步匹配，不能仅部署一端。
6. 增加全部主体统一查看、主体列、搜索筛选和进入具体主体；汇总页不允许确认人员、上传模板或生成报盘，具体业务操作仍按主体进行。
7. `vercel.json` 增加每天北京时间 08:00 的社保同步计划（UTC `0 0 * * *`），并增加配置回归检查。生产是否登记成功及实际执行结果仍须发布后核验。

## 尚未解决/验证

- 真正没有上传或配置的政务模板仍然缺失。当前自动模板库依赖本机目录，不能保证云端有文件；不能把审核清单或自造空表当作有效政府模板。需要对应主体、办理城市的有效模板才能做真实报盘验收。
- Vercel 连接工具未登录；已用现有 CLI 权限只读查询生产项目。指定 9 月 3 日历史区间返回 HTTP 400；最近三天的 `social-insurance` 日志查询未返回记录。不能据此推断根因或声称生产问题已修复。
- 已完成本地浏览器验证：使用 9 月 3 日生产发布的隔离副本，33 个主体、175 条人员记录；统一查看、搜索、待核对筛选、主体切换、汇总下载读回及桌面/手机布局通过。本地修改不回写生产，不能代替云端持久化验收。
- 该副本没有按新户籍规则重新计算；本地 8010 预览刻意关闭同步，不是实时北森环境。
- 新户籍规则用于新规则版本同步出的数据，不直接重写历史已确认名单。发布时需核对新同步结果，避免旧快照继续展示旧口径。

## 验证证据

```sh
node --test social_insurance_connector/test/*.test.mjs
uv run --offline --no-project --with pytest --with-requirements requirements.txt python -m pytest tests/test_social_insurance_*.py -q
node --check bonus_platform/static/social-insurance.js
git diff --check
```

2026-09-11 提交前复测：连接器 19 项通过；社保模块 171 项通过；JavaScript 语法及补丁空白检查通过。复测修正了页面已升为 v49、测试仍断言 v48 的缓存版本检查。测试包括生成真实 xlsx 后读回核对、混合周期拒绝、下载权限、确认名单写入范围、上传模板的文档读取识别，以及既有报盘包生成。

## 下一步

用户要求将本地全部最新改动一并交付集成窗口。基于最新实际生产版本选择性集成本提交，配套发布工作台与连接器；同步新规则数据，验证全部主体人数及审核结果、确认后刷新仍保留、实际模板上传及报盘文件下载，并核验定时计划及自动周期。

集成工作区 `/Users/zt27532/Documents/sigma-domestic-production-20260909` 已有本次相同的 `vercel.json` 和 `tests/test_social_insurance_serverless.py` 未提交补丁。集成前先检查其差异，妥善暂存后集成本提交，避免重复应用或覆盖无关修改。最近核实的生产版本为 `63ac5eca`；发布文档中的旧分支与实际生产来源有差异，集成窗口须确认实际生产基线并维护发布记录，不能直接从本开发分支覆盖生产。
